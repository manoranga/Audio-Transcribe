# GitHub project pages: app is served at https://<user>.github.io/<repo>/
# Must match the repository name (see Git remote). CI overrides with the repo from the workflow.
VITE_BASE_PATH=/Audio-Transcribe/
